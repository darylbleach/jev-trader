{
  "name": "jev-trader",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "license": "MIT",
  "devDependencies": {
    "@types/bun": "latest",
    "wrangler": "^4.135.0"
  },
  "peerDependencies": {
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "@ai-sdk/typesafe-ai": "^3.0.0",
    "@kuru-labs/kuru-sdk": "^0.0.95",
    "ai": "^7.0.103",
    "ethers": "5"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch run src/index.ts",
    "gold": "bun run src/gold/index.ts",
    "gold:dev": "bun --watch run src/gold/index.ts",
    "gold:backup": "bun run scripts/backup-gold-export.ts",
    "gold:deploy": "bun run scripts/gold-deploy.ts",
    "gold:cf": "bun run scripts/gold-deploy.ts",
    "gold:cf:dev": "wrangler dev --port 8787",
    "gold:cf:prepare": "bun run scripts/prepare-gold-cf.ts",
    "test": "bun test src/gold"
  }
}
